# Inspyra ERP Modules

This directory is the target home for the production React modules migrated from the visual prototype.

The first migration pass keeps the approved ERP interface intact in `src/erp/ERPPrototype.tsx` while the typed API, store, mocks, shared components, and route constants are prepared around it. Future passes can move each screen into its module directory without changing the user experience.
